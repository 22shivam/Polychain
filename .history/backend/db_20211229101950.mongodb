// uses the below database
use('multi-blockchain-payment-system');

// users collection is in the db
db.users.aggregate([
    {
        "$search": {
            "text": {
                "query": "shivam",
                "path": "username"
            }
        }
    }
])